/*
 * We create new package package also.
 * below is example of create myPackage.
 * in which we create similar type of files, sub-packages , similar type of classes.
 */
// package myPackage; 

import java.util.Scanner;

public class K_Package {
    Scanner sc = new Scanner(System.in);
    /*
     * Here scanner is class of util package.
     * we import util class. import java.util.*;
     * We import java package in which sub-package is util so we import util package.
     */
}
